<% layout('layouts/boilerplate') -%>

<h1>Sign In</h1>
<form class="needs-validation" action="/auth/login" method="POST" novalidate>
  <div class="mb-2">
    <label for="username" class="form-label">Username</label>
    <input
      type="text"
      class="form-control"
      name="username"
      id="username"
      required
      autocomplete="off"
    />
  </div>
  <div class="mb-2">
    <label for="password" class="form-label">Password</label>
    <input
      type="password"
      class="form-control"
      name="password"
      id="password"
      required
      autocomplete="off"
    />
  </div>
  <div class="mb-2">
    <button type="submit" class="btn btn-success">
      <span class="text-light fw-medium">Login</span>
    </button>
  </div>
</form>
